name: Docker Image CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Docker login
      env:
        DOCKER_USER: ${{secrets.DOCKER_USERNAME}}
      run: |
        echo ${{secrets.DOCKER_PASSWORD}} | docker login -u $DOCKER_USER --password-stdin
        
    - name: Build Docker image
      run: docker build ./client --file ./client/Dockerfile.dev --tag my-image-name:$(date +%s)
      
    - name: Push Docker image
      run: docker push ${{secrets.DOCKER_USER}}/bootcamp-multi-docker
